# 分摊记账软件 (ShareBill) - PRD 文档

## 1. 项目概述

### 1.1 产品名称
ShareBill - 智能分摊记账助手

### 1.2 产品定位
一款支持多种分摊模式（时间分摊、次数分摊、家庭分摊）的智能记账软件，帮助用户精确追踪日常开销，特别适合合租室友、家庭成员、项目团队等多人共同消费场景。

### 1.3 目标用户
- 合租室友（分摊房租、水电、日常用品）
- 家庭成员（家庭开销管理）
- 项目团队（团队费用分摊）
- 个人用户（精细化消费追踪）

### 1.4 核心价值主张
**"让每一笔分摊都清晰可见"**
- 支持复杂分摊场景（时间/次数/多人）
- 多维度统计分析（个人/家庭/标签）
- 跨平台使用（移动端 + Web）

---

## 2. 功能需求

### 2.1 核心功能模块

#### 2.1.1 记账模块
| 功能 | 优先级 | 描述 |
|------|--------|------|
| 基础记账 | P0 | 记录支出金额、名称、日期、分类 |
| 时间分摊 | P0 | 按天/周/月/年分摊（如：年费会员按365天分摊） |
| 次数分摊 | P0 | 按使用次数分摊（如：大米按食用次数分摊） |
| 标签系统 | P0 | 自定义标签（日常/电子产品/办公用品等） |
| 图片附件 | P1 | 支持上传发票、小票照片 |
| 语音记账 | P2 | 语音输入自动识别金额和类别 |

#### 2.1.2 家庭/群组模块
| 功能 | 优先级 | 描述 |
|------|--------|------|
| 创建家庭/群组 | P0 | 创建多人记账组 |
| 成员管理 | P0 | 邀请、移除成员，设置管理员 |
| 家庭记账 | P0 | 记录家庭公共支出 |
| 分摊规则 | P0 | 平均分摊、按收入比例、自定义比例 |
| 结算功能 | P0 | 计算成员间应收应付，生成结算方案 |
| 个人开销追踪 | P0 | 在家庭内追踪个人贡献和消费 |

#### 2.1.3 统计模块
| 功能 | 优先级 | 描述 |
|------|--------|------|
| 总览统计 | P0 | 月度/年度收支总览 |
| 标签统计 | P0 | 按标签分类统计（饼图、趋势图） |
| 分摊统计 | P0 | 查看各分摊项的累计金额 |
| 家庭统计 | P0 | 家庭总支出、成员贡献占比 |
| 趋势分析 | P1 | 支出趋势预测、异常提醒 |
| 导出报表 | P1 | 导出 Excel/PDF 报表 |

#### 2.1.4 用户模块
| 功能 | 优先级 | 描述 |
|------|--------|------|
| 注册/登录 | P0 | 手机号/邮箱/微信登录 |
| 个人中心 | P0 | 头像、昵称、默认货币设置 |
| 数据同步 | P0 | 多端数据实时同步 |
| 数据备份 | P1 | 云端备份、本地导出 |
| 隐私设置 | P1 | 家庭内可见性设置 |

---

## 3. 功能详细设计

### 3.1 记账流程

#### 3.1.1 普通记账
```
输入金额 → 填写名称 → 选择分类 → 选择日期 → 选择标签 → 保存
```

#### 3.1.2 时间分摊记账
```
输入金额 → 填写名称 → 选择"时间分摊" → 设置分摊周期：
  - 分摊方式：按天/按周/按月
  - 分摊时长：输入天数或选择起止日期
  - 开始日期：默认今天，可修改
→ 系统自动计算：日均金额 = 总金额 / 天数
→ 选择标签 → 保存
```

**示例：**
- 场景：购买年度视频会员 365元
- 设置：按天分摊，365天
- 结果：每天分摊 1元，持续一年

#### 3.1.3 次数分摊记账
```
输入金额 → 填写名称 → 选择"次数分摊" → 设置分摊参数：
  - 预计总次数：如 50次
  - 已使用次数：记录每次使用
→ 系统自动计算：单次金额 = 总金额 / 总次数
→ 每次使用后更新剩余次数和累计分摊金额
→ 选择标签 → 保存
```

**示例：**
- 场景：购买大米 100元，预计吃20次
- 设置：次数分摊，20次
- 结果：每次食用分摊 5元
- 功能：每次食用点击"+1次"，累计分摊金额实时更新

### 3.2 家庭/群组设计

#### 3.2.1 家庭数据结构
```
Family {
  id: string
  name: string
  members: [
    {
      userId: string
      role: 'admin' | 'member'
      nickname: string
      joinDate: timestamp
    }
  ]
  defaultSplitRule: 'equal' | 'income' | 'custom'
  createdAt: timestamp
}
```

#### 3.2.2 家庭记账流程
```
选择家庭 → 记账（同个人记账） → 选择分摊成员 → 选择分摊规则：
  - 平均分摊：所有成员均摊
  - 按比例：设置各成员分摊比例
  - 指定金额：指定各成员具体金额
→ 保存后自动计算各成员应付金额
```

#### 3.2.3 结算功能
```
功能入口：家庭详情页 → 结算
算法：
  1. 计算每个成员的"净贡献" = 支付总额 - 应承担总额
  2. 净贡献为正的成员应收到钱
  3. 净贡献为负的成员需要付钱
  4. 生成最小交易次数的结算方案

示例：
  成员A支付 500元，应承担 300元 → 应收 200元
  成员B支付 200元，应承担 300元 → 应付 100元
  成员C支付 100元，应承担 200元 → 应付 100元
  
结算方案：
  B 付给 A 100元
  C 付给 A 100元
```

### 3.3 标签系统设计

#### 3.3.1 标签结构
```
Tag {
  id: string
  name: string
  color: string (hex)
  icon: string
  type: 'system' | 'custom'
  userId: string (custom标签)
}

系统预设标签：
- 日常消费（餐饮、交通、购物）
- 电子产品
- 办公用品
- 娱乐休闲
- 医疗保健
- 教育培训
- 房屋相关（房租、水电、物业）
```

#### 3.3.2 多标签支持
- 一笔支出可打多个标签
- 统计时可按单个标签或多个标签组合筛选

---

## 4. 统计功能设计

### 4.1 统计维度

#### 4.1.1 时间维度
- 日视图
- 周视图
- 月视图
- 年视图
- 自定义时间范围

#### 4.1.2 分类维度
- 按标签统计
- 按分摊类型统计（普通/时间分摊/次数分摊）
- 按消费类型统计（个人/家庭）

#### 4.1.3 成员维度（家庭模式）
- 各成员支出占比
- 各成员贡献占比
- 成员间收支对比

### 4.2 图表类型
- 饼图：支出占比
- 柱状图：月度对比
- 折线图：趋势分析
- 列表：详细明细

---

## 5. 技术架构

### 5.1 技术栈选择

#### 5.1.1 前端
| 技术 | 选择 | 理由 |
|------|------|------|
| 框架 | Flutter 3.x | 跨平台（iOS/Android/Web）、Dart语言 |
| 状态管理 | Riverpod 2.x | 类型安全、性能优秀、测试友好 |
| 路由 | GoRouter | 声明式路由、深度链接支持 |
| 本地存储 | Hive/Isar | 高性能本地数据库 |
| 图表库 | fl_chart | Flutter生态主流图表库 |

#### 5.1.2 后端
| 技术 | 选择 | 理由 |
|------|------|------|
| 语言 | Node.js / Python / Go | 待讨论确定 |
| 框架 | NestJS / FastAPI / Gin | 根据语言选择 |
| 数据库 | PostgreSQL | 关系型数据、复杂查询 |
| 缓存 | Redis | 会话、热点数据 |
| 消息队列 | Redis/RabbitMQ | 异步任务 |

#### 5.1.3 部署
- 容器化：Docker + Docker Compose
- 云服务器：阿里云/腾讯云/AWS
- CI/CD：GitHub Actions

### 5.2 数据库设计

#### 5.2.1 核心表结构
```sql
-- 用户表
users (
  id UUID PRIMARY KEY,
  phone VARCHAR(20) UNIQUE,
  email VARCHAR(100) UNIQUE,
  nickname VARCHAR(50),
  avatar_url TEXT,
  default_currency VARCHAR(3) DEFAULT 'CNY',
  created_at TIMESTAMP
)

-- 标签表
tags (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  name VARCHAR(50),
  color VARCHAR(7),
  icon VARCHAR(50),
  is_system BOOLEAN DEFAULT false,
  created_at TIMESTAMP
)

-- 支出记录表
expenses (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  family_id UUID REFERENCES families(id) NULL,
  amount DECIMAL(10,2),
  currency VARCHAR(3) DEFAULT 'CNY',
  name VARCHAR(200),
  description TEXT,
  category VARCHAR(50),
  expense_date DATE,
  split_type VARCHAR(20) DEFAULT 'none', -- none, time, count
  split_config JSONB, -- 分摊配置
  tags UUID[],
  images TEXT[],
  created_at TIMESTAMP
)

-- 时间分摊详情表（用于记录每天的分摊金额）
time_split_details (
  id UUID PRIMARY KEY,
  expense_id UUID REFERENCES expenses(id),
  date DATE,
  amount DECIMAL(10,2),
  is_settled BOOLEAN DEFAULT false
)

-- 次数分摊记录表
count_split_records (
  id UUID PRIMARY KEY,
  expense_id UUID REFERENCES expenses(id),
  used_count INTEGER,
  used_date TIMESTAMP,
  note TEXT
)

-- 家庭/群组表
families (
  id UUID PRIMARY KEY,
  name VARCHAR(100),
  created_by UUID REFERENCES users(id),
  default_split_rule VARCHAR(20) DEFAULT 'equal',
  created_at TIMESTAMP
)

-- 家庭成员表
family_members (
  id UUID PRIMARY KEY,
  family_id UUID REFERENCES families(id),
  user_id UUID REFERENCES users(id),
  role VARCHAR(20) DEFAULT 'member', -- admin, member
  nickname VARCHAR(50),
  join_date TIMESTAMP
)

-- 家庭支出分摊记录表
family_expense_splits (
  id UUID PRIMARY KEY,
  expense_id UUID REFERENCES expenses(id),
  member_id UUID REFERENCES family_members(id),
  should_pay DECIMAL(10,2),
  paid DECIMAL(10,2) DEFAULT 0,
  split_ratio DECIMAL(5,2) -- 分摊比例
)
```

---

## 6. 页面结构

### 6.1 页面清单

#### 6.1.1 核心页面
| 页面 | 路由 | 描述 |
|------|------|------|
| 首页/仪表盘 | /home | 收支概览、快速记账入口 |
| 记账页 | /expense/add | 新增支出（普通/分摊） |
| 明细页 | /expenses | 支出列表、筛选、搜索 |
| 明细详情 | /expense/:id | 支出详情、编辑、删除 |
| 统计页 | /statistics | 图表统计、报表 |
| 家庭页 | /families | 家庭列表、创建 |
| 家庭详情 | /family/:id | 成员、支出、结算 |
| 结算页 | /family/:id/settlement | 结算方案 |
| 标签管理 | /tags | 标签列表、增删改 |
| 我的 | /profile | 个人中心、设置 |

### 6.2 页面原型（文字描述）

#### 6.2.1 首页
```
┌─────────────────────┐
│ ShareBill      [+]  │  ← 顶部栏，+号快速记账
├─────────────────────┤
│ 本月支出      ¥2,580│  ← 本月总支出
│ 剩余预算      ¥1,420│  ← 预算进度
├─────────────────────┤
│ 📊 支出趋势（折线图）│
├─────────────────────┤
│ 🏷️ 支出分类（饼图）  │
├─────────────────────┤
│ 📋 最近支出         │
│ • 午餐 ¥35        > │
│ • 地铁 ¥6         > │
│ • 超市 ¥128       > │
└─────────────────────┘
```

#### 6.2.2 记账页
```
┌─────────────────────┐
│ < 记一笔            │
├─────────────────────┤
│ ¥                   │  ← 金额输入
│ [0] [1] [2] [3]    │  ← 数字键盘
│ [4] [5] [6] [.]    │
│ [7] [8] [9] [完成] │
├─────────────────────┤
│ 名称：[早餐        ]│
│ 日期：[2026-02-13 ▼]│
│ 标签：[餐饮 ▼]      │
├─────────────────────┤
│ 分摊方式：          │
│ ○ 不分摊 ● 时间分摊 │
│ ○ 次数分摊          │
├─────────────────────┤
│ [保存]              │
└─────────────────────┘
```

---

## 7. 非功能需求

### 7.1 性能需求
- 页面加载时间 < 2秒
- 记账操作响应时间 < 500ms
- 支持离线记账，联网后自动同步

### 7.2 安全需求
- 用户数据加密存储
- 传输使用 HTTPS
- 敏感操作需验证身份

### 7.3 兼容性
- 移动端：iOS 12+, Android 8+
- Web端：Chrome 90+, Safari 14+, Edge 90+

### 7.4 可用性
- 支持中文/英文切换
- 深色模式支持

---

## 8. 项目规划

### 8.1 里程碑规划

| 阶段 | 时间 | 目标 |
|------|------|------|
| MVP | 4-6周 | 基础记账、时间分摊、标签、统计 |
| v1.0 | +2周 | 次数分摊、数据同步、Web端 |
| v1.5 | +3周 | 家庭功能、结算、导出 |
| v2.0 | +4周 | 图片附件、语音记账、趋势预测 |

### 8.2 风险识别
| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| Flutter Web 性能问题 | 中 | 早期验证，必要时降级功能 |
| 分摊算法复杂度高 | 中 | 充分测试边界情况 |
| 多成员并发编辑冲突 | 中 | 实现乐观锁/版本控制 |

---

## 9. 附录

### 9.1 术语表
- **时间分摊**：将一笔支出按天数均摊到多个日期
- **次数分摊**：将一笔支出按使用次数均摊
- **家庭**：多人共享的记账组
- **结算**：计算成员间应收应付并生成方案

### 9.2 参考竞品
- 随手记（基础记账）
- 挖财（家庭记账）
- Splitwise（国外分摊工具）
- 钱迹（简洁记账）

### 9.3 待确认事项
- [ ] 后端技术栈选择（Node.js/Python/Go）
- [ ] 是否支持多币种？汇率如何处理？
- [ ] 预算提醒功能是否需要？
- [ ] 是否需要接入支付功能（如AA收款）？

---

**文档版本**: v1.0  
**创建日期**: 2026-02-13  
**作者**: AI Assistant  
**状态**: 待评审
